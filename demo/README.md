# Demo

The canonical sample input is in `data/sample_global_impact_input.json`.

```bash
python3 python/global_impact_core.py \
  --input data/sample_global_impact_input.json \
  --output outputs/sample_global_impact_contract.json \
  --markdown outputs/sample_global_impact_brief.md
```

The WordPress demo is in `wordpress/global-impact-catalyst-demo/` and uses `[global_impact_catalyst_demo]`.
